#Bind Settings
bind branch L !git pull origin %(branch)
bind branch P !git push origin %(branch)
bind branch F !git flow feature start %(prompt)
bind branch H !git flow hotfix start %(prompt)
bind status C !git cz
